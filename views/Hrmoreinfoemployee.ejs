<% include ./partial/header %>

<div
  class="col-sm-9"
  style="margin: 50px 125px;    text-align: -webkit-center; border: 1px solid purple"
>
  <form
    action="/hr/<%=hr.id%>/leave/<%=employee.id%>/info"
    method="POST"
  >
    <div
      class="container-fluid bg-3 text-center"
      style="border: 1px solid blue"
    >
      <h1><%=employee.name %></h1>
      <br />
      <div class="row">
        <div class="col-sm-4">
          <img
            style="border-radius: 100%"
            src="<%=employee.image%>"
            alt="image"
          />
        </div>
        <%employee.leaves.forEach(function(leave){%>
        <div class="col-sm-4">
          <h3><strong> Subject:</strong><%=leave.subject %></h3>
          <h3>Status:<%=leave.hrstatus %></h3>
          <h3>From:<%=moment(leave.to).format('DD/MM/YYYY') %></h3>
          <h3>To:<%=moment(leave.from).format('DD/MM/YYYY') %></h3>
          <h3><%=leave.days%></h3>
          <h3>Designation:<%=employee.designation%></h3>
          <h3>Project:<%=employee.project %></h3>
          <div style="float:inherit">
            <input
              type="submit"
              class="btn btn-block btn-success"
              name="action"
              value="Approve"
              onClick="history.go(-1)"
            />
            <input
              type="submit"
              class="btn btn-block btn-danger"
              name="action"
              value="Decline"
              onClick="history.go(-1)"
            />
          </div>
        </div>

        <%})%>
      </div>
    </div>
  </form>
  <a href="/hr/<%= hr.id%>/leave">Back</a>
</div>
<% include ./partial/footer %>
